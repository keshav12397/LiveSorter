# Verification config for mainOfflineReplay.cu -- see that file's header for
# why it exists (SpikeGLX here needs a human in its Configure dialog before
# any run can start, and its NI stream cannot carry synthetic syllable
# codes, so the syllable -> decision -> trial -> DO-line chain has no live
# path to test against at all).
#
# D:/sim_validate/filters (12 units, ~28 Hz mean false-positive rate) rather
# than D:/sim_session/filters_baseline (160 units): the 160-unit bank's
# combined ~15k detections/s -- a symptom of the pre-fix synthetic
# generator's flat firing-rate defect (see README) -- exceeds what
# DecisionThread's polling loop can drain in real time, and every trial's
# wall-clock window closed before the matching spikes were even popped off
# spikeQueue (spike_count stuck at 0-1 despite ~100+ genuinely falling in
# window by sample index -- see this session's report for the numbers).
# This smaller bank's ~340 Hz combined rate lets DecisionThread keep up and
# was confirmed to: raise/lower the DO line 22 times over 37 syllables,
# with 22 kWireTrial(triggered=1)/13 kWireTrial(triggered=0) records
# independently verified on the wire by a from-scratch client (not
# LiveWireClient.cpp) against LiveWire.h's documented layout.
replayBinPath=D:/sim_validate/sim_g0_t0.imec0.ap.bin
carChannelMapJson=D:/test_newsorter/rawData/shank1only.json
filterDir=D:/sim_validate/filters
templateLength=61
filterNChannels=5
applyHighpass=true
highpassCutoffHz=300
imecSyncBit=6
imecSyllableBits=0,1,2
niDebounceSamples=10

decisionUnitIds=9,31,7,14,17,11,22,28,3,20,8,36
windowStartMs=-200
windowEndMs=500
spikeCountThreshold=2
doLine=Dev4/port0/line5
doPulseMs=50

spikeTimesPath=D:/scratch/offline_replay_spikeTimes.csv
syllableTimesPath=D:/scratch/offline_replay_syllableTimes.csv
decisionLogPath=D:/scratch/offline_replay_decisions.log
decisionCsvPath=D:/scratch/offline_replay_decisions.csv

viewerEnabled=true
viewerPort=4143

# Real time -- pruneExpired() closes trials on wall-clock time, so anything
# other than 1x makes the trigger outcomes meaningless (see
# mainOfflineReplay.cu's header). replayDurationSec=0 replays the whole
# 150 s file.
replaySpeed=1
replayDurationSec=0

sglxHost=localhost
sglxPort=4142
